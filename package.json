{
  "name": "entreskill-hub",
  "version": "1.0.0",
  "private": true,
  "description": "EntreSkill Hub - Skill-to-Startup Enablement Platform (test runner root)",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "dotenv": "^17.4.2"
  }
}
